/*
d[n] : 0부터 n까지의 정수 k개를 더해서 그 합이 n이 되는 경우의 수

*/

#include <iostream>
#include <vector>
using namespace std;

vector< vector<long long> > d(201, vector<long long>(201, 0));
const long long mod = 1000000000;

int main(void) {
	int n, k;
	cin >> n >> k;



	return 0;
}